[project]
name = "mnemoverse-mcp-agent"
version = "0.1.0"
description = "Memory over the Model Context Protocol: discover, write, read, and report outcomes"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=2.2.0",
    "openai>=1.97.0",
    "python-dotenv>=1.1.1",
    "rich>=14.0.0",
]
